# Example environment variables for Tail Number Lookup
# Copy this file to .env and update with your values

# Database path (optional - defaults to data/faa_aircraft.db in project root)
# FAA_DB_PATH=/path/to/faa_aircraft.db

# API server settings (optional - defaults shown)
# API_HOST=0.0.0.0
# API_PORT=8000

# Data directory (optional - defaults to data/ in project root)
# FAA_DATA_DIR=/path/to/data
